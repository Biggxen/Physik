% globale Einstellungen laden
\input{../config/global.tex}

% Abstand nach Liste
    \setlist[enumerate,1]{after=\vskip0\baselineskip, label= \alph*)}

% Nummerierung von enumitem ändern
    \setlist[enumerate,1]{label= \arabic*.}
    \setlist[enumerate,2]{label= \alph*)}